<h2><%= @doctor.name %></h2>
<h3><%= @doctor.department %></h3>
<p>
    <% @doctor.appointments.each do |appt| %>
        <%= link_to "#{appt.datetime} - #{appt.patient.name}", patient_path(appt) %>
    <% end %>
</p>